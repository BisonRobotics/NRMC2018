Things you'll need
----------------
pip install pytest
pip install pytest-cov

Run the tests
------------------
#You can do this from /NRMC2018/
pytest

Run the coverage tests
----------------------
pytest --cov=src/imperio/scripts
